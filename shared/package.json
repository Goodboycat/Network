{
  "name": "@network/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Network platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix"
  },
  "keywords": [
    "shared",
    "types",
    "typescript"
  ],
  "author": "Goodboycat",
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
